## Data directory

Place the dataset archive at `data/TotoEval.zip` so it can be downloaded from the homepage via the
relative link `data/TotoEval.zip`.

This directory is designed to remain stable as the website grows (e.g., adding `docs/`, `benchmark/`,
or `leaderboard/` at the repository root).
